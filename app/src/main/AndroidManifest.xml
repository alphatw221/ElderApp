<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.hardware.camera" />
    <uses-permission android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".myAccountActivity" android:launchMode="singleInstance"/>
        <activity android:name=".EventActivity" android:launchMode="singleInstance"/>
        <activity android:name=".HomeActivity" android:launchMode="singleInstance"/>
        <activity android:name=".give_money_comfirm_Activity" />
        <activity android:name=".give_money_Frag" />
        <activity android:name=".take_money_Frag" />
        <activity android:name=".TransactionActivity" />
        <activity android:name=".UpdateMyDataActivity" />
        <activity
            android:name=".TabActivity"
            android:label="@string/title_activity_tab"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".Http_Get"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".Http_Post"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>